apiVersion: v1
kind: Service
metadata:
  name: vllm-embedding
  namespace: default
spec:
  ports:
  - name: http-vllm-embedding
    port: 80
    protocol: TCP
    targetPort: 8000
  selector:
    app: vllm-embedding
  sessionAffinity: None
  type: ClusterIP 